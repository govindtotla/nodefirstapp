import Dashboard from '@views/SignUp';
import { Main as Layout } from '@layouts';

const Login = () => (
  <Layout>
    <Head>
      <title>Login</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>

    <SignUp />
  </Layout>
);

export default Login;
